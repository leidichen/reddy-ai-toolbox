# DeepSeek DeskBand

<p align="right">
  <a href="README.md"><img src="https://img.shields.io/badge/简体中文-Switch-blue?style=flat-square" alt="切换到简体中文" /></a>
  &nbsp;
  <img src="https://img.shields.io/badge/English-Current-brightgreen?style=flat-square" alt="Current: English" />
</p>

[![MIT License](https://img.shields.io/badge/License-MIT-green.svg)](./LICENSE)

> This project is forked from [leidichen/DeepSeek-DeskBand](https://github.com/leidichen/DeepSeek-DeskBand) with feature enhancements, licensed under MIT.
>
> Original code was generated by GitHub Copilot using the DeepSeek-V4 model.

> **Windows taskbar balance monitor** — displays your DeepSeek API balance in real time on the Windows taskbar.

## Taskbar Preview

![](https://img.lenband.com/{year}/{month}/{md5}.{extName}/PixPin_2026-05-20_14-26-53-Topaz-Gigapixel-2x-比例.png)

**Taskbar widget**

![](https://img.lenband.com/{year}/{month}/{md5}.{extName}/未标题-1-Topaz-Gigapixel-2x-比例.png)

## One-Click Build & Install

```powershell
# Open PowerShell 7 as Administrator, navigate to the project folder:
cd .
.\install.ps1
```

`install.ps1` automatically: cleans → generates strong-name key → builds → registers COM → restarts taskbar.

After installation, right-click the taskbar → **Toolbars** → check **"DeepSeek DeskBand"**, then left-click the widget → Set API Key.

---

## Full Uninstall

```powershell
# Run as Administrator:
.\uninstall.ps1
```

`uninstall.ps1` cleans **all 4 items**:

| Step | Clears |
|------|--------|
| COM unregister | `RegAsm /unregister` |
| Registry cleanup | `HKCR\CLSID\{...}` & ProgID |
| **API Key removal** | Windows Credential Manager `DeepSeekDeskBand:ApiKey` |
| Taskbar refresh | Restart Explorer |

> **!** Deleting files alone is not enough! You must run `uninstall.ps1` to remove COM registration and credentials.

---

## MSI Installer (Recommended)

One-click install, supports uninstall via **Settings → Apps → Apps & features**.

### Build MSI

```powershell
# WiX Toolset is required; the script will install it automatically
.\installer\build-installer.ps1
```

Output: `installer\DeepSeekDeskBand.msi`

### Install

1. Double-click `DeepSeekDeskBand.msi` → Next → Install
2. Right-click taskbar → **Toolbars** → check **"DeepSeek DeskBand"**
3. Left-click the widget → **Set API Key**

### Uninstall

**Settings → Apps → Apps & features → DeepSeek DeskBand → Uninstall**

> **!** The API Key in Credential Manager is cleared automatically on uninstall, but you can manually verify:
> ```batch
> cmdkey /list | findstr DeepSeek
> ```

### MSI Advantages

| Feature | MSI Installer | install.ps1 |
|---------|---------------|-------------|
| Appears in "Apps & features" | Yes | No |
| Automatic COM cleanup on uninstall | Yes | Yes |
| Automatic credential cleanup | Yes | Yes |
| Silent/bulk deployment | Yes (`msiexec /i`) | No |
| Group Policy distribution | Yes | No |

---

## Manual Build (without install.ps1 / MSI)

```batch
# 1. Generate strong-name key (once only)
sn -k StrongName.snk

# 2. Build
dotnet build -c Release

# 3. Register COM as Administrator
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\RegAsm.exe bin\Release\net48\DeepSeekDeskBand.dll /codebase

# 4. Restart Explorer
taskkill /f /im explorer.exe && start explorer.exe
```

Manual uninstall:
```batch
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\RegAsm.exe bin\Release\net48\DeepSeekDeskBand.dll /unregister
cmdkey /delete:DeepSeekDeskBand:ApiKey
taskkill /f /im explorer.exe && start explorer.exe
```

---

## Prerequisites

| Dependency | Download |
|------------|----------|
| .NET Framework 4.8.1 SDK | https://dotnet.microsoft.com/download/dotnet-framework/net481 |
| (Optional) Visual Studio 2022 | https://visualstudio.microsoft.com |

---

## Setting Up API Key

1. Left-click the widget on the taskbar → **Set API Key...**
2. Enter your DeepSeek API Key (`sk-...`)
3. Click **Test Connection** to verify
4. Click **Save** → Key is stored encrypted in Windows Credential Manager

## Features Overview

| Feature | Description |
|---------|-------------|
| Balance Display | Real-time `¥ xx.xx` shown on the taskbar |
| Auto Refresh | Queries every 30 seconds, no manual action needed |
| Low Balance Alert | Background turns **red** when balance drops below **¥10** |
| Status Indicator | Green=OK / Yellow=Not configured / Red=Error or low balance |
| Detail Panel | Click to view: Total / Topped-up / Granted balance |
| Top-Up Button | One-click to open [platform.deepseek.com/usage](https://platform.deepseek.com/usage) |
| Secure Storage | API Key saved in Windows Credential Manager, auto-cleaned on uninstall |
| MSI Installer | Standard uninstall via Windows “Apps & features” |

---

## Changelog

### v1.1.0 (2026-05-20)

- **Low balance alert**: taskbar background turns red when balance < ¥10; reverts automatically when recharged
- **Top-Up button**: new button in the detail panel opens the recharge page in your default browser
- **UI improvements**: bottom buttons pinned to window base with even three-button layout
- **Fixed refresh flicker**: no more brief “...” placeholder during balance refresh
- **Fixed high-DPI popup clipping**: Win32 physical coordinates used for positioning; fixes truncation at 150% DPI
- **Fixed taskbar white border**: removed Region clipping in favor of path fill

### v1.0.0

- Initial release by [leidichen/DeepSeek-DeskBand](https://github.com/leidichen/DeepSeek-DeskBand)


